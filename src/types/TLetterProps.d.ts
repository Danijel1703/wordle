export type TLetterProps = {
	domId: string;
	dispatch: Function;
	value: string;
	disabled: boolean;
	consists: boolean;
	isCorrect: boolean;
	isIncorrect: boolean;
	loading: boolean;
	onChange: Function;
};
